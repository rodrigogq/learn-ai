# Python
.venv/
__pycache__/
*.pyc

# Downloaded datasets and training outputs (can be gigabytes; every chapter
# regenerates them with its download/train scripts)
datasets/
checkpoints/
runs/
*.ckpt
*.safetensors

# Exported model weights for the C inference examples are regenerated by the
# corresponding Python export scripts
*.bin

# Compiled C output (Makefiles build into build/ inside each chapter's c/ folder)
build/
*.o
*.out

# Editors and OS
.DS_Store
*.swp

# Audio written by the chapter 18 C example
chord.wav
