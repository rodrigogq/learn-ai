# Core dependencies for all chapters.
# Chapters that need extra packages say so in their README.
# Versions are minimums known to work; newer versions are usually fine.
numpy>=2.5
torch>=2.13
torchvision>=0.28
matplotlib>=3.11

# Chapter 30 only (reinforcement learning environments). Optional: the
# gridworld runs without it; only the CartPole DQN needs it.
gymnasium>=1.0
